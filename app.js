// Sandwich Cooking Application

function makeSandwich(bread, filling) {
    return `Your sandwich with ${filling} on ${bread} is ready!`;
}

const sandwich = makeSandwich('whole grain', 'turkey');
console.log(sandwich);
